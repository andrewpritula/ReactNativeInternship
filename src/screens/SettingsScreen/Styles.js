import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({ 
  themeContainer: {
    alignItems: 'center',
  },
  title: {
    margin: 5,
    fontSize: 18,
    fontWeight: 'bold',
  },
});

export default styles;
