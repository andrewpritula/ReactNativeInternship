import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    padding: 10,
  },
  button: {
    padding: 10,
    alignItems: 'center',
    borderRadius: 2
  },
  buttonTitle: {
    fontSize: 20,
  },
});

export default styles;
